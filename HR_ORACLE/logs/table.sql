CREATE TABLE FUNCIONARIOS_AYM AS (SELECT * FROM EMPLOYEES)
 
SELECT * FROM FUNCIONARIOS_AYM

CREATE TABLE LOG_FUNCIONARIOS_AYM(
  ID_LOG NUMBER(10) PRIMARY KEY,
  EMPLOYEE_ID NUMBER(6) NOT NULL,
  SALARY_OLD NUMBER(8,2) NULL,
  SALARY_NEW NUMBER(8,2) NULL,
  DTH_REGISTRO DATE NOT NULL,
  USUARIO VARCHAR2(30) NOT NULL,
    CONSTRAINT FK_LOG_FUNCIONARIOS_AYM FOREIGN KEY(EMPLOYEE_ID)
      REFERENCES FUNCIONARIOS_AYM(EMPLOYEE_ID)
);
 
